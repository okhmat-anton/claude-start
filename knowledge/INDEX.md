# INDEX — оглавление базы знаний

Одна строка на единицу знания: что это, для какого стека, из какого проекта пришло.

Формат: `- [путь] — краткое описание (стек; from <проект>)`

- [skills/viewer-reviewer/SKILL.md] — ревью контента «глазами наивного зрителя» с подтверждением цели у автора (контент-проекты: видео, статьи, лендинги; from work-video-production)
- [lessons/api-discovery.md] — порядок поиска недокументированных эндпоинтов/полей REST API: вторичные доки → журнал уроков → живой GET → пробная запись с echo (любой проект на внешнем REST API; from work-video-production)
- [lessons/agent-shell-state.md] — PATH/cwd не персистятся между Bash-вызовами агента: export PATH + абсолютные пути, иначе файлы падают в корень репо (любой агентный CLI-харнесс; from work-video-production)
- [skills/standard-stack/SKILL.md] — единый стандарт стека, команд (make run/test/lint/build/update), именования, git и деплоя для всех проектов; вопросы-стандарты в /start больше не задаются (python+fastapi, vue/js, postgres, docker; from business-planner)
- [stacks/python-fastapi-vue.md] — конвенции backend/frontend: структура app/, pydantic-settings, auth-зависимости, SQLAlchemy 2.0, Celery, Vue 3, порты по сотне на проект (python fastapi + vue; from ai-video-production через business-planner)
- [stacks/kie-ai.md] — интеграция kie.ai: Bearer, createTask/recordInfo, ловушка «HTTP 200 с ошибкой в теле» (code != 200), ловушка №2 «не-стриминговые текстовые генерации >2 мин рубятся 500 — только SSE» (from gym-bro-2), вебхук/поллинг, билдеры пер-модель, приоритетные фото-модели, image-to-image через `input.image_input` с публичными URL (from gym-bro-2) (любой проект с kie.ai; from ai-video-production через business-planner)
- [lessons/mvp-discipline.md] — MVP-дисциплина прототипа: ядро прежде обвязки, жёсткие этапы, лёгкая инфраструктура на ноль пользователей, критерий «автор сам пользуется» (любой продуктовый прототип; from jym-bro-2)
- [lessons/ui-silent-failures.md] — UI никогда не игнорирует действие молча: интерактивный вид = работает, отказ всегда с откликом, долгие операции не держат busy-блокировку ввода (любой клиент, проверено на Flutter; from jym-bro-2)
- [lessons/multi-language-checks.md] — канонические make lint/test покрывают каждый язык репо (ruff+flutter analyze, pytest+flutter test); вторая команда «руками» = источник пропусков (любой мультиязычный репо; from jym-bro-2)
- [lessons/ai-video-pipeline.md] — ИИ-видеопродакшн: реверс-цепочка якорей и морф кадр-в-кадр (повторений: 2), лишние руки/лица в кадре и кроп вместо перегенерации (повторений: 2), консистентность элементов между клипами, звук ИИ-клипов как актив (эхо при дубляже), библиотеки ассетов «что осталось — то одобрено», версии text/clean, учёт времени/денег из API, ffmpeg-грабли склейки (в т.ч. xfade после concat: settb=AVTB + fps), приёмы-реверсы проверять на просмотре, референс-цепочка для консистентной серии кадров, пилот формата и «красота — не хук» (генеративное видео + ffmpeg; from work-video-production), ограничения площадки по продвижению перебивают текст в кадре (основная версия — без текста, домен только в подписи), цена/вопрос в первом кадре против даты и красоты, морф большого перепада описывать словами, i2v отдаёт неподвижный кадр на «медленный наезд» (лечится монтажным зумом), целостность файлов от платного API (ретрай + проверка декодируемости + исключения в потоках), товар-якорь из реального рендера каталога, цвет текста под фон серии, ревью партии одним прогоном (повторений: 2), пилот нового формата (повторений: 2)
- [lessons/agent-interaction.md] — контрольные точки длинной задачи: выбор кнопкой а не текстом, цена платной операции в самом вопросе, брак называть до заказчика (любой агент с многошаговым согласованием; from work-video-production)
- [lessons/llm-prompt-defaults.md] — рантайм-редактируемые промты в БД: правка дефолта без архивной копии не доезжает до прода; ревьювер работает только по явному перечню «REVISE, если …»; сгенерированный текст — не носитель метаданных (единицу/валюту хранить в структуре, подпись ИИ локализует) (любой продукт с LLM; from gym-bro-2)
- [lessons/prod-data-ops.md] — прод-данные правим ops-инструментами репо: read-only диагностика, идемпотентный ремонт с dry-run, точечные операции, «факт важнее плана»; диагностика показывает содержимое записей, а не заголовки (любой проект с доступом к прод-БД; from gym-bro-2)
- [lessons/workspace-visual-isolation.md] — окно каждого проекта помечается своим тёмным приглушённым Peacock-цветом: нет `.vscode/settings.json` — создать, есть — не трогать; перед выбором проверить занятые цвета соседних проектов (любой проект в VS Code; from hr)
- [stacks/python-selenium-agent-worker.md] — браузерные боты + агент-воркер поверх REST: API-first воркер, SQLite+APScheduler на дешёвом VPS, статус «sent» только по подтверждению страницы, скриншот+DOM на падении, селекторы не по памяти, идемпотентные задачи (python + selenium + fastapi; from hr)
- [lessons/external-agent-data.md] — данные от внешнего агента: два барьера (нормализация на записи + терпимость на чтении), одна кривая запись не роняет весь список; здоровьем считать запрос первого экрана, а не заголовок страницы; health, называющий конкретную битую запись (любой сервис, принимающий контент извне; from akm-news)
- [stacks/python-fastapi-mongo-nuxt.md] — вариант стандарта для контент-проектов: FastAPI + motor, Nuxt 3 SSR через routeRules (не devProxy), cookie через useRequestHeaders, тестовая БД форсируется до импорта app, точечный pytest (python fastapi + mongo + nuxt; from akm-news)
- [lessons/lint-adoption.md] — внедрение линтера в непроверявшийся код: правило «ноль зависимостей» про рантайм, а не про разработчика; два уровня проверок; первый прогон = сначала чинить конфиг под принятые в проекте приёмы, потом читать находки (любой проект, доживший до первого линтера; from planning)
- [lessons/agent-guard-hooks.md] — защитные хуки агента: разбирать команду программой с границами слов, а не шаблонами оболочки; блокировать изменение защищённого пути, а не упоминание; обязательный прогон на настоящих командах проекта до включения (любой агентный CLI-харнесс с хуками; from planning)
